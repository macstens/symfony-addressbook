{% extends 'base.html.twig' %}

{% block body %}

    {% block navbar %}
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="/" title="Made by Marcel Freitag">LILLYDOO Addressbook Application</a>
                </div>
                {% if is_granted('ROLE_ADMIN') %}
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a href="{{ path('contacts_list') }}" class="nav-link{% if app.request.attributes.get('_route') == 'contacts_list' %} active {% endif %}">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('logout') }}">Logout</a>
                        </li>
                    </ul>
                {% endif %}
            </div>
        </nav>
    {% endblock %}

    {% block content %}
    {% endblock %}

{% endblock %}